import {useState} from 'react';
const seed=[{id:1,name:'August Course Promotion',template:'course_reminder',audience:1240,status:'Scheduled',date:'Aug 30, 10:00 AM',sent:0,delivered:0,read:0},{id:2,name:'Welcome New Leads',template:'welcome_customer',audience:386,status:'Completed',date:'Aug 27, 6:00 PM',sent:386,delivered:372,read:241}];
export default function Campaigns(){const [items,setItems]=useState(seed);const [show,setShow]=useState(false);const [name,setName]=useState('');const add=()=>{if(!name.trim())return;setItems(x=>[{id:Date.now(),name,template:'Select template',audience:0,status:'Draft',date:'—',sent:0,delivered:0,read:0},...x]);setName('');setShow(false)};return <main className="campaigns-page"><header className="campaigns-head"><div><p className="eyebrow">MARKETING</p><h1>Campaigns</h1><p className="muted">Create, schedule and measure WhatsApp marketing campaigns.</p></div><button className="primary" onClick={()=>setShow(true)}>+ Create campaign</button></header><section className="campaign-stats"><div><strong>{items.length}</strong><span>Total campaigns</span></div><div><strong>{items.reduce((a,c)=>a+c.audience,0).toLocaleString()}</strong><span>Total recipients</span></div><div><strong>{items.reduce((a,c)=>a+c.delivered,0).toLocaleString()}</strong><span>Delivered</span></div><div><strong>{items.reduce((a,c)=>a+c.read,0).toLocaleString()}</strong><span>Read</span></div></section><section className="campaign-table card"><table><thead><tr><th>Campaign</th><th>Template</th><th>Audience</th><th>Status</th><th>Scheduled / sent</th><th>Delivery</th></tr></thead><tbody>{items.map(c=><tr key={c.id}><td><strong>{c.name}</strong></td><td>{c.template}</td><td>{c.audience.toLocaleString()}</td><td><span className={`campaign-status ${c.status.toLowerCase()}`}>{c.status}</span></td><td>{c.date}</td><td>{c.sent?`${Math.round(c.delivered/c.sent*100)}%`:'—'}</td></tr>)}</tbody></table></section>{show&&<div className="modal-backdrop"><div className="modal"><div className="modal-head"><h2>Create campaign</h2><button onClick={()=>setShow(false)}>×</button></div><label>Campaign name<input value={name} onChange={e=>setName(e.target.value)} placeholder="e.g. August promotion"/></label><label>Template<select><option>course_reminder</option><option>welcome_customer</option></select></label><label>Audience<select><option>All active contacts</option><option>WhatsApp contacts</option><option>Custom segment</option></select></label><div className="modal-actions"><button className="secondary" onClick={()=>setShow(false)}>Cancel</button><button className="primary" onClick={add}>Save draft</button></div></div></div>}</main>}
