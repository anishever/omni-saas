import { useState } from 'react';

export default function ChannelSettings(){
 const [connected,setConnected]=useState(false); const [name,setName]=useState(''); const [phoneId,setPhoneId]=useState(''); const [token,setToken]=useState('');
 const save=()=>{if(name&&phoneId&&token)setConnected(true)};
 return <main className="settings-page"><p className="eyebrow">CHANNELS</p><h1>WhatsApp</h1><p className="muted">Connect your WhatsApp Business number to receive and send customer messages.</p><section className="settings-card"><div className="settings-title"><div><h2>WhatsApp Cloud API</h2><p>Meta WhatsApp Business Platform</p></div><span className={`connection-pill ${connected?'connected':''}`}>{connected?'Connected':'Not connected'}</span></div><hr/><label>Connection name<input value={name} onChange={e=>setName(e.target.value)} placeholder="e.g. Main Business WhatsApp"/></label><label>Phone Number ID<input value={phoneId} onChange={e=>setPhoneId(e.target.value)} placeholder="Meta Phone Number ID"/></label><label>Permanent Access Token<input type="password" value={token} onChange={e=>setToken(e.target.value)} placeholder="Paste your Meta access token"/></label><div className="settings-help">Your access token is stored encrypted. Never share it in chat or source code.</div><div className="settings-actions"><button className="secondary">Test connection</button><button className="primary" onClick={save}>{connected?'Save changes':'Connect WhatsApp'}</button></div></section><section className="setup-card"><h2>Webhook configuration</h2><p>After connecting, configure this callback URL in Meta Business Manager and use your verification token.</p><code>/api/v1/webhooks/whatsapp</code><p className="muted">Inbound messages will automatically appear in the Unified Inbox.</p></section></main>
}
