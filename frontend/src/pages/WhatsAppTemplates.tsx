import { useState } from 'react';

type Template={id:number;name:string;language:string;category:string;status:string;body:string};
const seed:Template[]=[{id:1,name:'welcome_customer',language:'en_US',category:'UTILITY',status:'Approved',body:'Hi {{1}}, welcome to {{2}}! How can we help you today?'},{id:2,name:'course_reminder',language:'en_US',category:'MARKETING',status:'Pending',body:'Hi {{1}}, your session starts at {{2}}.'}];
export default function WhatsAppTemplates(){const [items,setItems]=useState(seed);const [show,setShow]=useState(false);const [name,setName]=useState('');const [body,setBody]=useState('');const add=()=>{if(!name.trim()||!body.trim())return;setItems(x=>[...x,{id:Date.now(),name,language:'en_US',category:'MARKETING',status:'Draft',body}]);setName('');setBody('');setShow(false)};return <main className="templates-page"><header className="templates-head"><div><p className="eyebrow">WHATSAPP</p><h1>Message Templates</h1><p className="muted">Create and manage approved WhatsApp templates for customer messaging and campaigns.</p></div><button className="primary" onClick={()=>setShow(true)}>+ Create template</button></header><section className="template-grid">{items.map(t=><article className="template-card" key={t.id}><div className="template-top"><span className="category">{t.category}</span><span className={`template-status ${t.status.toLowerCase()}`}>{t.status}</span></div><h2>{t.name}</h2><small>{t.language}</small><div className="preview">{t.body}</div><div className="template-foot"><span>{(t.body.match(/\{\{\d+\}\}/g)||[]).length} variables</span><button className="secondary">⋯</button></div></article>)}</section>{show&&<div className="modal-backdrop"><div className="modal"><div className="modal-head"><h2>Create template</h2><button onClick={()=>setShow(false)}>×</button></div><label>Template name<input value={name} onChange={e=>setName(e.target.value)} placeholder="e.g. appointment_reminder"/></label><label>Category<select><option>MARKETING</option><option>UTILITY</option><option>AUTHENTICATION</option></select></label><label>Message body<textarea value={body} onChange={e=>setBody(e.target.value)} placeholder="Hi {{1}}, your appointment is confirmed for {{2}}."/></label><p className="muted">Use variables like {{'{{1}}'}} and {{'{{2}}'}} for personalized values.</p><div className="modal-actions"><button className="secondary" onClick={()=>setShow(false)}>Cancel</button><button className="primary" onClick={add}>Save draft</button></div></div></div>}</main>}
